<!DOCTYPE html>
    <html>
        <head>
            <style>
                fieldset {
                background-color: #fae3e3;
                }

                legend {
                background-color: #4e0000;
                color: white;
                padding: 5px 10px;
                }

                input {
                margin: 5px;
                }

                input[type=submit] {
                width: 20%;
                background-color: #4e0000;
                color: white;
                padding: 6px 20px;
                border: none;
                border-radius: 4px;
                }

                button {
                margin: 5px;
                }

                button[type=submit] {
                width: 20%;
                background-color: #4e0000;
                color: white;
                padding: 6px 20px;
                border: none;
                border-radius: 4px;
                }
            </style>
        </head>
    <body>
<%= yield %>
    </body></html>